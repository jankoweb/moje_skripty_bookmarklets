javascript:void( document.body.onclick = async function (event) { event = event || window.event; var element = event.target || event.srcElement; await new Promise(resolve => setTimeout(resolve, 10)); state = 'Czechia'; index = document.body.innerText.search(state); place = document.body.innerText.substring(index - 4,index).match(/\n([0-9]+)/); alert(state + ' is #' + place[1] + ' in ' + element.textContent + ' worldwide.')} );
